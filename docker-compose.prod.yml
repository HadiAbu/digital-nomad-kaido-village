version: '3.8'

services:
  app:
    image: ghcr.io/hadiabu/digital-nomad-kaido-village:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    command: pnpm start